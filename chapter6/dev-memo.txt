truffle unbox react .. react UIと統合された新しいプロジェクト作成

状態変数を private で宣言。
状態変数を 読み取るために public getter関数を宣言。

------------------------------------------------------------------

配列や文字列などの参照型のデータを保持するパラメータでは、
保存されているデータを直接指しているのか（storage）、
データのローカルコピーを指しているのか（memory）
を指定する必要がある。


ほとんどの場合はmemory。


calldata .. 最後の格納場所。　関数をexternalで宣言するときに必要。

------------------------------------------------------------------
フォールバック関数 .. 無名関数。　パラメータなしのexternal関数。 payableも良い。
→ 匿名の寄付など。
・コントラクトが通常のトランザクションを通してイーサを受け取った時
・コントラクトが定義ずみのどの関数とも一致しないシグネチャを持つ関数で呼び出された時。


